package com.jedaway.game;

/**
 * A transition from one Position or game state to another.
 *
 * Move implementations are expected to override {@code equals} and {@code hashCode}
 */
public interface Move {
}
